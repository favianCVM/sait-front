version: "3.8"

services:
  app:
    container_name: app-dev
    image: app-dev
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      target: development
    volumes:
      - ./src:/app/src
    ports:
      - 3002:3000